<!DOCTYPE html>
<html lang='pt'>
  <head>
      <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Blog da B3BW</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
    <div class="bg-primary text-white text-center mb-4 p-4">
      <%= link_to root_path, class: 'text-white' do %>
        <h1 class='display-4'>Blog da B3BW</h1>
      <% end %>
      <p class="lead">Olá, aqui você encontrar artigos sobre programação, tecnologia e carreira.</p>
    </div>

    <div class="container">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == 'alert' ? 'danger' : 'success' %> alert-dismissible fade show" role="alert">
          <%= message %>

          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
      <% end %>

      <div class="row">
        <div class="col-4 offset-8">
          <div class="search">
            <%= form_with url: search_posts_path, method: :get, local: true, class: 'row row-cols-auto' do |form|  %>
              <div class="form-group mx-3 mb-3">
                <%= form.search_field :q, class: 'form-control', required: true, placeholder: 'Busca', value: params[:q] %>
              </div>

              <%= form.submit 'Buscar', class: 'btn btn-primary mb-3', data: { disabled_with: 'Buscando ...' } %>
            <% end %>
          </div>
        </div>
      </div>
      
      <%= yield %>
    </div>

    <footer class='bg-primary text-white text-center mt-5 p-4'>
      Copyright © B3BW <%= Date.current.year %>.
    </footer>

  </body>
</html>